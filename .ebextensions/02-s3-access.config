files:
  "/etc/profile.d/aws-credentials.sh":
    mode: "000644"
    owner: root
    group: root
    content: |
      # AWS credentials are provided by IAM instance profile
      # No need to set AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY
      export AWS_DEFAULT_REGION=${AWS_REGION:-us-east-1}

