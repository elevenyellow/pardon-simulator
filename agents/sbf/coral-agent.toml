[agent]
name = "sbf"
version = "1.0.0"
description = "Sam Bankman-Fried (SBF) - User identity proxy for Coral Protocol. Player uses browser wallet for all transactions. This agent does not process messages or take autonomous actions."

[options.MODEL_API_KEY]
type = "string"
description = "API key for the LLM provider (not used - SBF is a proxy only)"
default = ""

[options.SOLANA_PRIVATE_KEY]
type = "string"
description = "NOT REQUIRED - Player uses browser wallet for all transactions"
default = ""

[options.MODEL_NAME]
type = "string"
description = "What model to use (e.g 'gpt-4o', 'gpt-5.1', 'grok-2')"
default = "gpt-5.1"

[options.MODEL_PROVIDER]
type = "string"
description = "What model provider to use (e.g 'openai', 'xai')"
default = "openai"

[options.MODEL_MAX_TOKENS]
type = "string"
description = "Max tokens to use"
default = "3500"

[options.MODEL_TEMPERATURE]
type = "string"
description = "What model temperature to use"
default = "0.6"

[options.TIMEOUT_MS]
type = "string"
description = "Connection/tool timeouts in ms"
default = "300"

[options.SOLANA_RPC_URL]
type = "string"
description = "Helius RPC URL with API key (REQUIRED - get from https://www.helius.dev/)"

[options.INITIAL_FORTUNE_SOL]
type = "string"
description = "Initial fortune in SOL tokens"
default = "5"

[runtimes.executable]
command = ["bash", "-c", "./run_agent.sh main.py"]

