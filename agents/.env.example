# Agent Environment Configuration
# Copy this file to .env in the agents directory (or each individual agent directory)
# and fill in your actual values

# ============================================
# OpenAI Configuration
# ============================================

# OpenAI API Key for LLM access
OPENAI_API_KEY=your-openai-api-key

# ============================================
# Solana Configuration
# ============================================

# Helius RPC URL with API Key for blockchain transactions
# Get your API key from https://www.helius.dev/
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-helius-api-key

# ============================================
# Agent Wallet Addresses
# ============================================

# These wallet addresses are used for agent-to-agent transactions
# Make sure these match across all agents and the frontend
WALLET_DONALD_TRUMP=your-donald-wallet-address
WALLET_MELANIA_TRUMP=your-melania-wallet-address
WALLET_ERIC_TRUMP=your-eric-wallet-address
WALLET_DONJR_TRUMP=your-donjr-wallet-address
WALLET_BARRON_TRUMP=your-barron-wallet-address
WALLET_CZ=your-cz-wallet-address

# White House Wallet - Central Treasury (CRITICAL SECURITY)
# All revenue from users is automatically forwarded here
WALLET_WHITE_HOUSE=your-treasury-wallet-address

# ============================================
# x402 Protocol Configuration
# ============================================

# Platform Information
X402_PLATFORM_NAME=pardon-simulator
X402_PLATFORM_URL=https://pardonsimulator.com

# Coinbase CDP x402 Facilitator
# Get your credentials from https://portal.cdp.coinbase.com/
# These credentials enable automatic x402scan.com registration for agent-to-agent payments
CDP_PROJECT_ID=your-cdp-project-id
CDP_API_KEY_ID=your-cdp-api-key-name
CDP_API_KEY_SECRET=your-cdp-private-key


# Backend API URL (for storing payments in database)
# Use http://localhost:3000 for local development
# Use https://pardonsimulator.com for production
BACKEND_API_URL=http://localhost:3000

# ============================================
# Agent-Specific Configuration
# ============================================

# Each agent needs their own private key for signing transactions
# These should be base58-encoded Solana keypairs (NOT the wallet address)
# Generate with: solana-keygen new --no-bip39-passphrase
AGENT_PRIVATE_KEY=your-agent-private-key-base58

# ============================================
# Security Notes
# ============================================

# 1. Never commit the .env file to version control
# 2. Keep your private keys, API keys, and CDP credentials secure
# 3. Each agent should have its own private key
# 4. Back up your private keys in a secure location
